<p id="notice"><%= notice %></p>

<h1>Categoria</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categoria.each do |categorium| %>
      <tr>
        <td><%= categorium.nome %></td>
        <td><%= link_to 'Editar', edit_categorium_path(categorium), class:"btn btn-primary" %></td>
        <td><%= link_to 'Excluir', categorium, method: :delete,  class:"btn btn-danger",
                                            data: { confirm: 'Você tem certeza que deseja excluir?'} %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Categoria', new_categorium_path, class: "btn btn-success" %>
